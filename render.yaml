services:
  - type: web
    name: soma-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: STEP_DELAY
        value: "0.3"
    healthCheckPath: /health
